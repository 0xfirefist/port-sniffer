// main will get the input about the target info
// it will initialize the target struct
// and then it will call target.scan

fn main() {
    println!("Port Sniffer")
    // main will use parse-args to parse arguments
    // parse-args will return a target struct
    // target struct will have method implemented scan that will be used to scan
}